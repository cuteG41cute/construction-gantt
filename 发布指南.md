# 发布指南 / Release Guide

这份文档写给"要把它发到 GitHub"的人（也就是你自己）。

---

## 一、发之前必须确认的事

### ⚠️ 单位合规（最重要）

如果你在单位做工程相关工作，**发布前请确认**：

- [ ] 员工手册 / 保密协议里，有没有"对外发布技术成果"的条款？
- [ ] 这个工具里有没有单位的、项目的、客户的敏感信息？
- [ ] 你的直属领导知情吗？

> 本仓库的源码已经**清除了所有项目痕迹**（详见第六节的自查方法），
> 但**发布这个动作本身**是否合规，只有你能判断。

### ✅ 内容自查

- [ ] 源码里没有真实项目名 / 地名 / 人名（用第六节的交互式脚本自查）
- [ ] 没有 `autosave.json` 之类的真实数据文件
- [ ] 没有单位内部文件的拷贝

---

## 二、准备本地仓库

在 `施工排期甘特图_开源版` 文件夹里打开终端（PowerShell / CMD / Git Bash）：

```bash
cd "施工排期甘特图_开源版"

# 初始化仓库
git init

# 确认 .gitignore 生效（下面这条应该【没有输出】）
git status --short | findstr /i "autosave cache 版本快照"

# 添加文件
git add .

# 看一眼将要提交什么（确认没有数据文件！）
git status

# 首次提交
git commit -m "初始提交：施工排期甘特图工具 v1.1.0"
```

**关键检查**：`git status` 里**不应该出现** `autosave.json`、`cache_*.json`、`*.exe`。

---

## 三、在 GitHub 建仓库

1. 登录 GitHub，点右上角 **+** → **New repository**
2. 填写：
   - **Repository name**：`construction-gantt` 或 `施工排期甘特图工具`（英文更容易被搜到）
   - **Description**：`给工程人的、双击就能用的施工排期工具`
   - **Public**（公开）
   - **不要**勾选 "Add a README file"（我们已经有了）
3. 点 **Create repository**
4. 按页面提示把本地仓库推上去：

```bash
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

---

## 四、发布第一个版本（自动打包）

**推一个 tag，GitHub 会自动帮你打包 exe 并创建 Release。**

```bash
# 打标签（版本号要和源码里的 VERSION 一致）
git tag v1.1.0

# 推送标签（这一步会触发自动构建）
git push origin v1.1.0
```

然后：

1. 打开你的仓库 → **Actions** 标签页
2. 会看到 "构建并发布" 正在运行（黄色圆点）
3. 等 3~5 分钟（Windows 构建比较慢）
4. 变绿后，去 **Releases** 页面 —— 已经自动生成了 `v1.1.0`，附件里有 exe

> 💡 **原理**：`.github/workflows/release.yml` 里定义了流程 ——
> GitHub 开一台 Windows 机器 → 装 Python → 打包 → 上传到 Release。
> 这样别人下载的 exe 是 **GitHub 官方构建**的，比你自己传的更让人放心。

### 手动触发（不发版，只测试打包）

仓库 → **Actions** → 左侧选 "构建并发布" → **Run workflow** →
构建完在 Artifacts 里能下载 exe（30 天后过期）。

---

## 五、之后的日常更新

```bash
# 改完代码后
git add .
git commit -m "修复：xxx"
git push

# 要发新版本时
# 1. 改源码里的 VERSION = "1.1.1"
# 2. 提交
# 3. 打标签推送
git tag v1.1.1
git push origin v1.1.1
```

Release 会自动生成，附件里带上新 exe。

---

## 六、安全检查命令（每次发布前跑一遍）

在 `施工排期甘特图_开源版` 目录下：

> ⚠️ **不要把单位名、项目名、同事名字写进本文件**（因为本文件本身也会被公开）。
> 检查词请临时输入，或用下面的交互式脚本。

**Windows PowerShell（交互式，自己输入关键词）：**
```powershell
# 逐个输入你要检查的词（如单位名、项目名、同事姓名），回车后用空行结束
$words = @()
while ($true) {
    $w = Read-Host "要检查的词（直接回车结束）"
    if ([string]::IsNullOrWhiteSpace($w)) { break }
    $words += $w
}
$found = $false
Get-ChildItem -Recurse -File -Exclude "发布指南.md" | ForEach-Object {
    $c = Get-Content $_.FullName -Raw -ErrorAction SilentlyContinue
    foreach ($w in $words) {
        if ($c -like "*$w*") { Write-Host "命中: $w -> $($_.Name)"; $found = $true }
    }
}
if (-not $found) { Write-Host "OK 未发现敏感词" }

# 检查是否有数据文件混进来
Get-ChildItem -Recurse -Include "autosave*.json","cache_*.json","*.exe"
# 上面这条应该没有输出
```

**Git Bash / Linux（交互式）：**
```bash
printf "要检查的词（直接回车结束）："
while read -r w; do
    [ -z "$w" ] && break
    grep -rl "$w" . 2>/dev/null | grep -v "发布指南.md" && echo "命中: $w"
done
ls autosave*.json cache_*.json *.exe 2>/dev/null
```

---

## 七、目录结构说明

发布到 GitHub 后，仓库里应该是这样：

```
仓库根目录/
├─ .github/
│   └─ workflows/
│       └─ release.yml        ← 自动打包配置（GitHub 用）
├─ .gitignore                 ← 排除数据文件（重要！）
├─ gantt_tool.py              ← 主程序（唯一源码）
├─ README.md                  ← 中英双语说明
├─ LICENSE                    ← MIT 协议
└─ 发布指南.md                 ← 本文件
```

**exe 不在仓库里** —— 它在 Releases 页面，由 GitHub Actions 生成。

---

## 八、常见问题

**Q：为什么不能直接把 exe 提交到仓库？**
A：① 35MB 会让仓库变大、克隆变慢；② Git 每次改动都存历史，二进制文件会越滚越大；
③ GitHub 的标准做法就是"源码进仓库、成品进 Releases"。现在有 Actions 自动打包，更没必要手动传。

**Q：Actions 构建失败了怎么办？**
A：看 Actions 页面的日志。常见原因：
- `pyinstaller` 或 `openpyxl` 装不上 → 检查 `release.yml` 里的 pip 步骤
- 中文文件名在 Windows runner 上出问题 → 日志里会有具体报错

**Q：想改成英文仓库名/英文 exe 名？**
A：改 `release.yml` 里 `--name` 参数，以及 README 里的下载说明。

**Q：想让别人也能贡献代码？**
A：保持 MIT 协议，在 README 里加一句"欢迎 PR"就行。可以再加 `CONTRIBUTING.md`。

---

## 九、许可证提醒

仓库用的是 **MIT**，意思是：
- ✅ 别人可以随便用、改、商用
- ✅ 必须保留你的版权声明
- ✅ 你不承担任何责任（出问题不找你）

如果你想限制"必须开源衍生作品"，可以换成 **GPL-3.0**。
如果你只是自用不想别人商用，那就别用开源协议，或者用 **CC BY-NC**（但这不是标准软件协议）。

---
